This Web Application is developed to manage and track warehouse assets using QR code scanning at the dispatch and 
receiving warehouse even without internet or mobile in the users device.

The HTML creates the interface → JavaScript controls the interface → Apps Script processes/stores the data.

